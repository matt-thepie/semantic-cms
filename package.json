{
  "name": "semantic-cms",
  "version": "0.1.0",
  "description": "A CMS where an LLM acts as the semantic translation layer between a block editor and accessible, mobile-first HTML",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --env-file=.env server.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3",
    "bcrypt": "^5",
    "better-sqlite3": "^12.10.0",
    "better-sqlite3-session-store": "^0",
    "express": "^4",
    "express-rate-limit": "^7",
    "express-session": "^1",
    "multer": "^1.4.4-lts.1",
    "nodemailer": "^6",
    "sharp": "^0.33"
  },
  "devDependencies": {
    "dotenv": "^16"
  }
}
